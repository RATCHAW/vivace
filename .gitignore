# Dependencies and generated output
node_modules/
dist/
.next/
# Regenerated by `next typegen`; it references files inside .next/
next-env.d.ts
.turbo/
*.tsbuildinfo

# Local secrets. Keep deliberately redacted examples under version control.
.env
.env.*
!.env.example
!**/.env.example
!.env.*.example
!**/.env.*.example
*.local
.envrc
.direnv/
.dev.vars
.npmrc

# Credentials and local state that must never be committed
*.key
*.pem
*.p12
*.pfx
*.jks
*.keystore
*credentials*.json
*service-account*.json
*.tfstate
*.tfstate.*
*.sqlite
*.sqlite3
*.db

# Tool and workspace state
.context/
.vercel/
.wrangler/
.netlify/
.DS_Store

# Scratch output of `pnpm --filter @repo/api auth:generate` — read, never applied.
apps/api/.better-auth-schema.ts

# Playwright's own output: the HTML report, and the traces, videos and
# screenshots it keeps from a failed run.
apps/e2e/playwright-report/
apps/e2e/test-results/
apps/e2e/blob-report/
apps/e2e/.playwright/
